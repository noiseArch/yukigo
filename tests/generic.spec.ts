import { Analyzer, InspectionRule } from "../src/index.js";
import { assert } from "chai";

describe("Generic Spec", () => {
  let analyzer: Analyzer;
  beforeEach(() => {
    analyzer = new Analyzer();
  });
});
